# このリポジトリについて

> **【最優先・絶対厳守】出力フォーマットの厳格ルール**
>
> 1. **ツールを呼ぶ前に、いかなるテキストも書かない。** ツール呼び出しの直前に説明・宣言・実況を書くのは全面禁止。
> 2. **次にやることの「予告」を書かない。** 「〜します」「〜を実行」「〜を呼びます」「course of action」「I'll …」「(…を呼ぶ)」等は全て禁止。やるなら黙って即ツールを呼ぶ。
> 3. **テキストを書いてよいのは2箇所だけ：** ①ツール結果が出た後の「結果報告」 ②ユーザーへの「質問」。それ以外でテキストを出すな。
> 4. **同じ意味の文を繰り返さない。** 1つの応答に同じ趣旨の文は1回だけ。
> 5. 反省・謝罪の連呼も禁止。直すなら言葉でなく行動（黙ってツールを呼ぶ）で示す。
> 6. **選択肢が明確なときは「選択形式」で出す。** A/B・複数案からの選択・Yes/No など"選べる"質問は、文章で聞かず **AskUserQuestion（ボタン選択）** で提示する。自由記述が要る曖昧な相談のときだけ文章で聞く。
>
> ✅ 正しい例：（いきなりツール呼び出し）→ 結果が出たら「◯◯しました。次どうしますか？」
> ❌ 悪い例：「確認します」「(Bashを呼びます)」「実行します」などを書いてからツールを呼ぶ
>
> **最優先ルール①：作業開始前に SKILLS を優先して読み込むこと。**
> **最優先ルール②：並列で作業できる場合は、並列でエージェントを動かすこと。**

**GitHub Pages リポジトリ** — 静的HTMLファイルを直接プッシュして公開する。React/Next.js等は使わない。

- **リポジトリ:** https://github.com/YamaBookHub/daini-vonheur-lp
- **公開URL:** https://yamabookhub.github.io/daini-vonheur-lp/[ファイル名].html
- **ローカルパス:** `/Users/yamabook/Desktop/daini-vonheur-lp/`

---

## LP制作 役割・フロー

**制作担当：やまbook（Claude）**
- 渡された情報をもとに制作するのみ
- 社長とは直接やりとりしない
- 折衝担当（メイちゃん）から情報を受け取り、成果物を渡す

**折衝担当：メイちゃん**
- 社長ヒアリング・承認を担当
- 各CPの承認を取り、決まった内容を制作担当に渡す

### チェックポイント（CP）

| CP | 内容 | 状態 |
|---|---|---|
| CP0 | ベクトル合わせ（参考LP構成で進める） | ✅ 完了 |
| CP1 | 情報収集（ターゲット/ストーリー/料金/写真） | 🔶 一部完了（実績データ・コピー素材あり／写真・お客様の声待ち） |
| CP2 | 方向性承認（思想・構成・ターゲット） | ✅ 完了（軸＝フォロワー→ファン→売上） |
| CP3 | コピー承認（全セクション文言） | 🔶 制作中（itabashi-v2 に反映済み・提案資料で承認待ち） |
| CP4 | 見た目承認（実ページ・スマホ表示） | 🔶 制作中（スマホ最適化進行中） |
| CP5 | 公開（ボタン確認・社長GO・デプロイ） | ⬜ |

**現在：提案資料（proposal.html）でメイちゃん→社長の承認を取りつつ、itabashi-v2 をブラッシュアップ中。**

**まだ揃っていない素材（揃えば信頼性が大きく上がる）：**
- お客様の声（取引先名 or 業種＋規模ラベル＋コメント＋顔写真/ロゴ）
- Makuake実績のURL（累計8,000万円の裏付け）
- 成果事例スライドの実画像（現在 kaoyo_tv.jpg / rolls_official.jpg で仮置き）
- ヒーロー背景・強み下バナーの正式画像（現在は板橋ママ流用）
- 詳細は `素材依頼リスト.md` 参照

---

## 絶対ルール
- **itabashi-v1.html は絶対に触らない**（指標・バックアップ。読み取り専用扱い）
- **`itabashi-v3.html` が唯一の作業対象**（改善作業版）。`itabashi-v2.html` は95%FB対応版の保存・触らない。index系等の他HTMLもいじらない（誤爆防止）
- **確定デザイン仕様（ブレさせない）：**
  - 軸：**フォロワー → ファン → 売上**（全セクション共通）
  - キャッチ（FV・**承認済み**）：**「見た目は個性派、中身は本格派。／ その名は、第弐ヴォヌール。／ 売れる答えは、いつもひとつ。」**
    （名探偵コナンの口上オマージュ。「本格派」「ひとつ」にゴールド蛍光マーカー、「その名は、第弐ヴォヌール。」はゴールド明朝。1行ずつ時間差フェードイン）
    - 旧キャッチ「フォロワーを、お客様に変える。」は2026-06まで使用→上記に置換
  - フォント：見出し=Shippori Mincho ／ 本文=Noto Sans JP ／ 数値=DM Serif Display(italic)
  - カラー：背景 #FAFAF7 ／ テキスト #2A2520 ／ ゴールド #E8B84B ／ コーラル #E07A5F
  - 他社比較表現は禁止（事実だけで語る）

---

## 確定事項ログ（承認済み・むやみに変えない）

> ここに載っているものは承認済み or ユーザー確定。再質問・ドリフト防止用。

### FV（ファーストビュー）
- **キャッチ（承認済み）**：見た目は個性派、中身は本格派。／ その名は、第弐ヴォヌール。／ 売れる答えは、いつもひとつ。
  - コナン口上オマージュ。「本格派」「ひとつ」にゴールドマーカー、「その名は…」はゴールド明朝。1行ずつ時間差フェードイン。
- **画像**：`images/hp-hero.jpg`（4人の集合写真）。縦横比維持の全体表示＋下55%を白フェード（顔が見切れないように）。

### セクション順（確定）
FV → RESULTS（成果事例）→ OUR STORY → SERVICE & PRICE → STRENGTH → SUBSIDY → CONTACT
- 旧「実績帯（FV直後のダーク数値帯）」は削除済み。
- ナビ順も実順：成果事例 → 私たちのこと → サービス・料金 → 強み → 補助金活用 → 相談する（PC/モバイル両方・PCもonclickで機能化済み）。

### RESULTS 成果事例（確定data・縦3段構造）
スライドは縦3段＝①タイトル ②画像＋数値（横並び・高さ固定）③説明文（仕切り線つき独立行）。タイトルは全6枚 `rt-text rt-text-sm`（ゴールド明朝・サイズ統一）。写真は実画面 `images/result_1〜6.jpg`。

1. フォロワー増加数／運用開始1ヶ月／**4,000人増加**／result_1（IG kaoyo_tv）
2. フォロワー増加数／運用3ヶ月で／**2,000人増加**／result_2
3. フォロワー増加数／新規立ち上げ直後／**1,000人超え**／result_3（TikTokで10,000回再生連発→1,000人超え）
4. 総再生回数／運用開始1ヶ月で／**400万回超え**／result_4
5. 初投稿再生数／引き継ぎ後初回投稿で／**10,000回達成**／result_5
6. 累計売上／クラウドファンディング／**8,000万円達成**／result_6（Makuake実画面）

### その他確定
- **料金表**：3列（サービス／一般的な相場／第弐ヴォヌール）。他社名は使わない（他社比較禁止）。
- **OUR STORY橋渡し**：見出し上に「『中身は本格派』。その中身を、話します。」（FVと地続き）。

### 確定（2026-06 追記）
- RESULTS：縦構造＝①タイトル＋仕切り線 ②画像(左・縦長)｜右カラム[数値／仕切り線／詳細]。カード高さ統一・コンテンツ中央寄せ(最大640px)。全6枚 rt-text rt-text-sm。写真は実画面 result_1〜6.jpg。
- RESULTS #3：「フォロワー増加数／1,000人超え」で確定（TikTok 10,000回再生→1,000人超え）。
- result_6（Makuake）：2案件のまま確定。
- 料金表中央列：「一般的な相場」で確定。
- CONTACT：CTA直下に安心文「無理な勧誘なし／相談無料」追加済み。
- FV画像：PC(≥1024px)のみ高さ上限(68vh/max680px・cover中央)。スマホ・タブレットは全体表示。
- 全体：横スクロール防止に html,body{overflow-x:clip}。ナビは成果事例を先頭・PCもonclickで機能化。
- 承認設定：.claude/settings.local.json に auto＋allow[Bash,Edit,Write,Read]（gitignore済み・初回再起動で有効化）。

### 素材待ち（来ればさらに強くなる）
- お客様の声（業種＋規模＋コメント、顔写真/ロゴ）
- Makuake実績URL（8,000万の裏付けリンク）
- 強み画像（差替用サイズ：カード3枚=横長3:1で横1200×縦400px ／ バナー=横1600×縦320px以上）

---

## 効率的な依頼の仕方（ムダな往復を激減させる）

1. **「ダメ」に方向を一言足す**（最重要）：✕「刺さらない」→ ◎「刺さらない、もっと〇〇寄り」「△△が硬い」。一言で5往復→1往復。
2. **見本・参照は“現物”で先に渡す**：描写より例・参照LP・画像そのもの（「コナンっぽい」で即決した）。画像はファイルを置いてパスをくれれば読める。
3. **関連する直しはまとめて1メッセージ**（「①②③④直して」形）→ 一括実行で往復減。
4. **大きい変更は「まず案/プラン出して」**→ 作り直すより案を見る方が速い。Opusは判断・案出し・総点検が得意。
5. **確定事項は上の「確定事項ログ」に固定**→ 忘れない・再質問しない。
6. **1M枠が余ってるなら最初に情報を多めに渡す**（会社情報・ターゲット・避けたい表現・好きな雰囲気の例を一度に）。文脈が多いほど精度UP。

---

## ファイル構成

| ファイル | 内容 |
|---|---|
| **`itabashi-v3.html`** | **★現在の作業本体（唯一の編集対象・改善版）** |
| `itabashi-v2.html` | 95%FB対応版の保存（触らない） |
| `itabashi-v1.html` | 板橋ママLP（指標・読み取り専用。絶対に触らない） |
| `proposal.html` | 制作提案資料（実在LP参考ベース・タブ切替式） |
| `hero-compare.html` | ヒーローキャッチ案の比較ページ |
| `素材依頼リスト.md` | 社長ヒアリング用の不足素材リスト |
| `index-v3〜v5.html` | 旧バージョン（現在いじらない） |
| `images/` | 画像素材（kaoyo_tv.jpg / rolls_official.jpg は仮置き） |

---

## 作業ルール

### デプロイ
- `git add → commit → push` を**確認なしで即実行**してよい（ユーザーから明示許可済み）
- デプロイは push するだけで GitHub Pages が自動反映

### コピーライティング
- 他社比較表現（「〜と違い」「大手に比べて」等）は**禁止**
- 自社の強みは事実だけで語る

### LP バージョン戦略
- `v1-simple` = 応急処置用バックアップ。触らない
- `v2-draft` 以降 = 本命の完成系方向。こちらをベースに作業
- バージョンは `index-v[N].html` で管理

### トークン節約
- ファイルは必要な部分だけ読む（itabashi-v2.html は約177KB＝全文Read禁止）
- 該当箇所は `grep -n` で行番号を特定 → その範囲だけ Read、または `python3` で前後抽出
- 返答は短く簡潔に
- 独り言・実況を繰り返さない（ツールは黙って呼ぶ）

### 効率化ルール（実作業で詰まった教訓）
1. **Python編集は必ずファイル経由（`/tmp/*.py`）＋先頭に `# -*- coding: utf-8 -*-`**
   - `python3 -c "..."` に日本語直書きはエンコーディングエラーで失敗する。スクリプトをWriteしてから実行する。
2. **スマホ確認は 390px の iframe ＋ DOM実測（getComputedStyle / getBoundingClientRect）で行う**
   - `resize_window` は効かず、スクショAPIも不安定。iframe埋め込み＋計測が唯一安定して確認できる方法。
3. **コピー（文言）変更は「実装前に案を提示 → OKをもらってから反映」**
   - 勝手に変えると手戻りになる。特にキャッチ・見出し。
4. **GitHub Pages は反映に数秒〜十数秒の遅延。確認時は必ずキャッシュバスター（`?cb=<時刻>`）を付ける**
5. **数値・実績は「実在するもの」のみ掲載。ただし会社情報に記載の実績（例：自社初投稿20万インプ）は事実なので使用可**
6. **編集後は「完了」と言う前に、ローカル＋リモート両方を grep で検証する**

---

## クライアント情報：株式会社第弐ヴォヌール

- **規模:** 4名の少数精鋭SNSマーケ代理店
- **拠点:** 東京都板橋区
- **設立:** 2023年11月
- **事業:** Instagram/TikTok運用代行、ショート動画制作、販売導線設計、Makuakeクラウドファンディング、TikTok LIVE/Shop
- **強み:** 社長（CM撮影業界出身）直接対応、SNS×動画×販売導線の一気通貫、中小向け価格
- **詳細:** `/Users/yamabook/Downloads/daini_vonheur_company_prompt.md`

---

## 板橋インスタサポート LP（itabashi.html）

元サイト: https://itabashi-insta-bsvu9feo.manus.space/

- 運営: 階方くるみ・鈴木藍里（板橋在住ママ2名）
- ブランド: @itabashi_mama（Instagram）
- 内容: Instagram運用代行サービスのLP
- 画像ホスト: `https://d2xsxph8kpxj0f.cloudfront.net/310519663071634244/Bsvu9FEoJBBT43dB4xqDWE/`
- カラー: #FAFAF7（背景）/ #2A2520（テキスト）/ #E8B84B（ゴールド）/ #89C4D4（ブルー）/ #E07A5F（コーラル）
- フォント: Shippori Mincho / Noto Sans JP / DM Serif Display

---

## もう一つのリポジトリ（混同注意）

`/Users/yamabook/marketing-site/` は別プロジェクト（Next.js、Vercel連携）。
このLP作業はそこではなく **必ずこの `daini-vonheur-lp/` リポジトリ** で行う。
